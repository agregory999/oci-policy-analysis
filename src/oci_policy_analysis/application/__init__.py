"""Application-level service layer for OCI Policy Analysis.

This package contains orchestration services that sit between the core logic
and consumer interfaces (Tk, web, CLI, MCP). It is intentionally framework-
agnostic and contains no UI or HTTP dependencies.
"""
